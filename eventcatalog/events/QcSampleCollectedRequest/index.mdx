---
id: QcSampleCollectedRequest
name: QcSampleCollectedRequest
version: '1.0'
summary: Event published by qualitycontrol service when qcsamplecollectedrequest occurs
owners:
  - qualitycontrol-service
producers:
  - qualitycontrol-service
consumers:
  - qualitycontrol-service
badges:
  - content: "Schema Valid ✓"
    backgroundColor: "#22c55e"
    textColor: white
  - content: "Domain: Manufacturing"
    backgroundColor: "#3b82f6"
    textColor: white
  - content: "v1.0"
    backgroundColor: "#6366f1"
    textColor: white
---

# QcSampleCollectedRequestEvent

**Schema ID**: 127
**Schema Version**: 2
**Subject**: QcSampleCollectedRequestEvent
**Domain**: Manufacturing
**Namespace**: com.arcone.biopro.manufacturing.qualitycontrol.domain.event

## Business Context

Event published when qcsamplecollectedrequest occurs

## Event Schema Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `eventId` | `uuid` | Yes | Unique identifier for this event |
| `occurredOn` | `timestamp-millis` | Yes | Timestamp when event occurred |
| `occurredOnTimeZone` | `string` | No | Timezone for occurredOn |
| `eventType` | `string` | No | Type of event |
| `eventVersion` | `string` | No | Version of the event schema |
| `unitNumber` | `string` | No | unitNumber field |
| `productCode` | `string` | No | productCode field |
| `machineType` | `string` | No | machineType field |
| `procedureType` | `string` | No | procedureType field |
| `performedBy` | `string` | No | performedBy field |
| `filtrationEnd` | `string` | No | filtrationEnd field |
| `createDate` | `string` | No | createDate field |


## Sample Event

```json
{
  "eventId": "32f59280-9a46-4533-9115-c2e1fdbe0d5c",
  "occurredOn": 1763355987775,
  "eventType": "STANDARD",
  "eventVersion": "1.0",
  "machineType": "STANDARD",
  "procedureType": "STANDARD",
  "performedBy": "performedBy_value",
  "filtrationEnd": "filtrationEnd_value",
  "createDate": 1763355987775
}
```

## Schema Evolution

This event uses Avro schema validation through Confluent Schema Registry.

- **Schema ID**: 127
- **Version**: 2
- **Compatibility**: BACKWARD (default)

## Message Format

Messages are serialized using **Avro** with the schema ID embedded in the message header (magic byte + schema ID).

## Integration Notes

### Consuming This Event

When consuming this event, use the Confluent Avro deserializer which will:
1. Read the schema ID from the message
2. Fetch the schema from the registry (with caching)
3. Deserialize the message using the schema

### Publishing This Event

When publishing this event:
1. Use the Confluent Avro serializer
2. The serializer will register the schema if needed
3. The schema ID will be embedded in each message

## Download Schema

- [Download Avro Schema](./QcSampleCollectedRequestEvent.avsc) - Complete Avro schema definition

<NodeGraph />
