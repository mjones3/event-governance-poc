---
id: FlagUnitForResearch
name: FlagUnitForResearch
version: '1.0'
summary: Event published by eventmanagement service when flag unit for research occurs
owners:
  - eventmanagement-service
producers:
  - eventmanagement-service
consumers:
  - inventory-service
badges:
  - content: "Schema Valid ✓"
    backgroundColor: "#22c55e"
    textColor: white
  - content: "Domain: Donor"
    backgroundColor: "#3b82f6"
    textColor: white
  - content: "v1.0"
    backgroundColor: "#6366f1"
    textColor: white
---

# FlagUnitForResearchEvent

**Schema ID**: 68
**Schema Version**: 1
**Subject**: FlagUnitForResearchEvent
**Domain**: Unknown
**Namespace**: com.arcone.biopro.donor.eventmanagement.domain.event

## Business Context

Event published when flag unit for research occurs

## Event Schema Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `eventId` | `uuid` | Yes | Unique identifier for this event |
| `occurredOn` | `timestamp-millis` | Yes | Timestamp when event occurred |
| `occurredOnTimeZone` | `string` | No | Timezone for occurredOn |
| `eventType` | `string` | No | Type of event |
| `eventVersion` | `string` | No | Version of the event schema |


## Sample Event

```json
{
  "eventId": "8e779727-27b6-4970-9862-1c61fba7c473",
  "occurredOn": 1763355987306,
  "occurredOnTimeZone": 1763355987306
}
```

## Schema Evolution

This event uses Avro schema validation through Confluent Schema Registry.

- **Schema ID**: 68
- **Version**: 1
- **Compatibility**: BACKWARD (default)

## Message Format

Messages are serialized using **Avro** with the schema ID embedded in the message header (magic byte + schema ID).

## Integration Notes

### Consuming This Event

When consuming this event, use the Confluent Avro deserializer which will:
1. Read the schema ID from the message
2. Fetch the schema from the registry (with caching)
3. Deserialize the message using the schema

### Publishing This Event

When publishing this event:
1. Use the Confluent Avro serializer
2. The serializer will register the schema if needed
3. The schema ID will be embedded in each message

## Download Schema

- [Download Avro Schema](./FlagUnitForResearchEvent.avsc) - Complete Avro schema definition

<NodeGraph />
